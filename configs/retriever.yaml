# configs/retriever.yaml

type: "pg_hybrid"
embed_model_path: "BAAI/bge-small-en-v1.5" # Lightweight model for fast embedding
weights:
  semantic: 0.7  # Priority given to vector search
  keyword: 0.3   # FTS acts as a precise keyword booster

